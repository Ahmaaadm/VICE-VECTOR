# Copy to .env and customise.
# Only VITE_-prefixed vars are exposed to the client bundle.

# Leave empty in dev — the Vite proxy in vite.config.ts forwards /api to :5035.
# In production, point this at your deployed backend's URL.
VITE_API_BASE_URL=
